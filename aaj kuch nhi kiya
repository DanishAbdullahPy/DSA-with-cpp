#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;#include.<stdio.h>#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;#include.<stdio.h>
int main ()
{#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}
#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;#include.<stdio.h>#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;#include.<stdio.h>
int main ()
{#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}


int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;#inc

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;#include.<stdio.h>#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;#include.<stdio.h>
int main ()
{#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}
#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;#include.<stdio.h>#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;#include.<stdio.h>
int main ()
{#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}


int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;#inc

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}


















}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

















#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}


















}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}



















    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}


















}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

















#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}


















}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

















#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}


















}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

















#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}


















}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}



















    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}


















}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

















#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}


















}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}


















int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}


















}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

















#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of n
int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}


















}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

















#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}


















}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}



















    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}


















}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

















#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}


















}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

















#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}


















}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

















#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}


















}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}



















    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}


















}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

















#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}


















}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}


















int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}


















}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

















#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}


#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}


return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}



#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

    return 0;
}




int main() {
    int a, b;
    cin >> a >> b;

    // English representations of numbers 1 to 9
    string numbers[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through the range from 'a' to 'b'
    for (int i = a; i <= b; i++) {
        if (1 <= i && i <= 9) {
            // 'i' is between 1 and 9, print the English representation
            cout << numbers[i - 1] << endl;
        } else if (i > 9 && i % 2 == 0) {
            // 'i' is greater than 9 and even
            cout << "even" << endl;
        } else if (i > 9 && i % 2 != 0) {
            // 'i' is greater than 9 and odd
            cout << "odd" << endl;
        }
    }

 




#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

#include.<stdio.h>
int main ()
{
int kuch nhi kiya=5
cout << kuch nhi kya<< endl;
retyrn o;
}

aaj hamne padha while loop if else loop kiya question uspe based 
switch case
aur kiya hackerrank pe questions 

#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // English representations of n